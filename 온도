#include <stdio.h>
#define convert(x) ((9.0/5.0)*x)+32

int main() {
	float a, b, c;

	printf("섭씨 온도 세 개를 입력하시오");
	scanf_s("%f %f %f", &a, &b, &c);

	a = convert(a);
	b = convert(b);
	c = convert(c);

	printf("%,4f, %,4f %,4f\n", a, b, c);

	return 0;
}
